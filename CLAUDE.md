# aish

CLI SSH + AI (Claude Code)。ローカルシェル または SSH接続先サーバを、クライアント側のClaude Codeから調査・操作する対話型ツール。

## 開発環境
- 言語: Rust
- ビルド: `export PATH="$HOME/.cargo/bin:$PATH" && cargo build`
- 対応OS: Linux (Ubuntu), macOS, Windows（UI部はUnix限定、Windowsは `read_line_cooked` フォールバック）
- CI: `.github/workflows/ci.yml` が全 push で `cargo fmt --all -- --check` / `cargo clippy --all-targets -- -D warnings` (ubuntu) と `cargo test` (ubuntu + macOS) を回す。**push 前にこの 3 つをローカルで通すこと**。`release.yml` (タグ push でのリリース) とは独立。
  - **テスト実行は `cargo test`。`--lib` を付けない**: aish は bin-only crate (`src/lib.rs` なし) なので `cargo test --lib` はターゲット不在で 0 件になる。
  - **clippy `-D warnings` は構造的 lint を一部 `#[allow(clippy::...)]` で抑制している** (`utf8_char_len` の `if_same_then_else`、minibuffer/echo の `write_with_newline`、`compute_visual_layout` の `needless_range_loop`、minibuffer 関数群の `too_many_arguments`、入力スレッドの `while_let_loop`)。いずれも trust-critical / 意図的なコードを温存するためで、**安易に外して writeln! 化やリファクタをしない** (CLAUDE.md「実装上の注意」の write! 形固定等と整合させるため)。

## 仕様

詳細な仕様（アーキテクチャ、動作モード、UI要素、キー入力、AI連携、リングバッファ、スレッド構成、設定ファイル、エラー挙動、既知の制約など）は **[SPEC.md](./SPEC.md)** を参照。

## 信頼の根幹

aish は SSH でサーバを管理する道具なので、**ユーザが画面で承認したコマンド = サーバで実行されるコマンド** を保つこと、**サーバ側に勝手な書き込みをしないこと** が大原則。

具体的に避けるべき行為:
- AI 提案コマンドをラップして別の文字列に変形する（マーカーラッパ等）
- `PROMPT_COMMAND` / `precmd` / `set +o history` で shell 環境を黙って書き換える
- `HISTCONTROL=ignorespace` 依存等の「履歴に残さない工夫」
- 任意の shell 統合シーケンスの自動セットアップ

完了判定や exit code 取得が必要でも、**passive 検出**（PTY 出力を観察するだけ）の範囲で実現する。それで取れない情報は諦める。

## 実装上の注意

コードから直ちに読み取れない、間違えやすいポイント：

- **rawモードはセッション全体で維持**する（`save_terminal_settings` で設定）。`passthrough` / `read_confirm_key` 個別での再設定・復元は不要。
- **以降の注意点で言う「bash readline」は readline / emacs 互換の行編集を持つ対話シェルの意味**で、bash だけでなく **zsh の emacs モード (= macOS のデフォルト) も含む**。ローカルシェルは `$SHELL` で起動する (`src/pty_handler.rs`; `/bin/bash` は未定義時の fallback のみ) ので、macOS では zsh がそのまま立ち上がり追加対応は要らない。プロンプト戻り検出も `%`/`#` を終端集合に含む (`src/prompt_sniffer.rs`) ので zsh で機能する。**唯一の例外は打ちかけ消去 `0x01,0x0b` (Ctrl+A+Ctrl+K)** で、これは emacs 行編集に依存するため **zsh を vi モード (`bindkey -v`) にしている場合のみ** 劣化する (`^A^K` がリテラルで残る / 折り返し打ちかけが綺麗に畳まれない = vim 等の TUI 子プロセスへ届いたときと同じ穏当な失敗モード。クラッシュや未承認コマンド実行には至らない)。詳細は SPEC.md § 14。
- **低レベル端末入力の framing は `src/input.rs` に集約されている (唯一の場所)**。`ByteSource` trait から 1 byte ずつ読み、`next_event` が UTF-8 組み立て / ESC・CSI・SS3 解析 / poll+timeout を行って `InEvent { raw, tok }` を返す。`read_confirm_key` / `passthrough_read_raw` / `read_minibuffer_line` の 3 つはこの `Tok` を消費するだけの薄い層。**新規コードで fd 0 を直接 (`ManuallyDrop::from_raw_fd(0)`) 読む実装を増やさないこと** (現状の例外は `drain_stdin_nonblocking` と `query_cursor_position_dsr` の 2 つだけ。これらは passthrough が止まっている間にメイン/同スレッドが読むので競合しないが、将来 input 経由に寄せる余地あり)。**`raw` (元バイト列) が主役で `tok` (分類) は副**: passthrough は必ず `ev.raw` をそのまま PTY に送り、**`Tok::Char` を再エンコードして送ってはいけない** (invalid UTF-8 / Alt+非ASCII / paste / マウスシーケンスで壊れる)。focus event (`ESC[I`/`ESC[O`) は decoder では `Tok::FocusIn/FocusOut` として返し、捨てるかどうかは消費側 (passthrough だけが破棄、従来挙動) が決める。byte→Tok の対応は `src/input.rs` の golden test 群で固定。ここを変えたらテストも更新する。**minibuffer の CSI 解析が partial sequence で blocking してハングし得た旧バグは、framing が全 poll 化されて解消**。
- **minibuffer は bracketed paste マーカー (`ESC[200~`/`ESC[201~` = `Tok::PasteStart`/`PasteEnd`) を honor する**。マーカー間の改行 (`Tok::Enter`) は送信ではなく `\n` としてバッファに挿入し (= 複数行ペーストが最初の改行で途中送信されない)、ペースト外の本物の Enter だけが送信する。CRLF (`\r\n`) は `ev.raw` で CR/LF を判別して 1 つの `\n` に正規化。ペースト本文中の他トークン (Esc/Ctrl/矢印等) は誤爆防止のため無視する (改行と文字以外は挿入しない; Tab 等はドロップ)。**aish 自身は bracketed paste を有効化しない** (`ESC[?2004h` を送らない = 端末状態を変えない原則)。bash 等の shell が readline で有効化済みのマーカーをそのまま利用する。そのため **shell が bracketed paste を無効化している環境 (古い bash / dash 等) では複数行ペーストは従来どおり最初の改行で送信される** (既知の制約。堅牢化するなら minibuffer 表示中だけ `ESC[?2004h` を出す案があるが、出口で OFF に戻すと直後の bash ペーストが壊れるため ON のまま残す必要がある)。passthrough はマーカーも raw 転送するので inner program のペーストは不変。
- **AI 提案コマンドの確認プロンプト (Y/n/a) は 1 キー即確定** (`read_confirm_key`, `src/ui.rs`)。Enter は不要。低レベルの byte 読み・UTF-8 組み立て・ESC 解析は `crate::input` の `next_event` に集約されており、`read_confirm_key` は返ってきた `Tok` を解釈するだけ (`Tok::Char(c)` を `match_confirm_char` に通し、`Tok::Enter`/`Tok::Esc`/`Tok::Ctrl(0x03|0x04)` を個別処理)。受理する文字は ASCII `y`/`Y`/`n`/`N`/`a`/`A` + IME 全角 `ｙ`/`Ｙ`/`ｎ`/`Ｎ`/`ａ`/`Ａ` + ひらがな `あ` (= "a" の自然 IME 確定) と `ん` (= "n" 確定の自然出力)。Enter (`\n`/`\r`) と Space はデフォルト Yes (旧 `parse_confirm` の「空入力 = Yes」を踏襲)。**Enter が制御文字フィルタに飲まれる順序トラップ (過去 2 回再発) は `input::next_event` 側で構造的に解消済み**: Enter (`0x0a`/`0x0d`) は `b < 0x20` 判定より先に `Tok::Enter` に分類され、`src/input.rs` の byte→Tok golden test (`enter_is_not_swallowed_by_control_filter` 等) が回帰を防ぐ。Space (`0x20`) は `Tok::Char(' ')` として届き `match_confirm_char` が Yes 判定する。未知キーは **無視して再読み取り** (打ち間違いで意図せず No になる事故を避けるため)。Ctrl+C / Ctrl+D / ESC 単独は「残り全部キャンセル」(`InputEvent::ReadLineCancelled`)。**キャンセル 3 種 (Ctrl+C / Ctrl+D / ESC) は抜ける前に必ず stdout へ `\n` を 1 つ出す**。これをしないと、直後にメインループが送るシェルプロンプトのリフレッシュ (bash の `\r` + プロンプト文字列。先頭の改行は drain 側で除去される) が、カーソルがまだ `Exec? … [Y/n/a] ` 行末にあるためその行を上書きして消す (ユーザ報告: キャンセルで最終行がプロンプトに上書きされる)。`n`/`y` は `echo_confirm` の末尾 `\n` で改行が入るのでクリーンだったが、**ESC だけ旧実装が「echo はしない」で改行が無く上書きしていた**。3 種を同じ arm にまとめて `\n` を揃える (`Tok::Eof` は stdin 切断 = 退出間際なので対象外)。**echo はマッチした入力 char をそのままの大小で 1 文字 + `\n` で手動描画する** (raw mode は ECHO off)。これは「ユーザが押したキー = 画面に映る文字」を保つため (`y` 押下なら `y`、`Y` 押下なら `Y`、全角 `ｙ` やひらがな `あ` も押下通り)。Enter (`\n` / `\r`) は入力 char が無い byte 段で先取り処理しており、デフォルト Yes の視覚表現として `'Y'` を固定 echo する。Space (`0x20`) は UTF-8 デコード経由なので入力 char ` ` がそのまま echo され視覚的にはほぼ空白行になるが、`echo_confirm` 内に case 分岐を入れない方針なのでトレードオフとして許容する (Space で Yes 確定するユースケースは稀)。`echo_confirm` 自体は `match` を持たず `write!("{c}\x1b[0m\n")` だけ。「大小区別 echo」のためにこの関数に分岐を足さないこと (= 分岐を足すと「ユーザの押下が常に大文字化される」旧バグ復活と見分けが付かない実装になりがち)。
- **IME の未確定文字 (preedit) は aish からは取得不能**。fcitx / mozc / Windows IME / macOS IME 等は OS の入力メソッド層で preedit buffer を保持しており、確定 (commit) されるまで stdin に 1 バイトも流れてこない (terminal emulator が overlay 描画しているだけで PTY には到達しない)。Kitty keyboard protocol / CSI u を有効化しても変わらない (IME が下流)。したがって「IME 入力中にリアルタイム反応」は構造上不可能で、確定済み文字でマッチするのが現実解。`read_confirm_key` はその方針 (= IME 入力中は何キーを叩いても aish には届かないので無反応に見える) で実装している。
- **stdin の termios は `c_lflag` の `ICANON|ECHO|ISIG|IEXTEN` に加えて `c_iflag` の raw 化フラグ群 (`IGNBRK|BRKINT|PARMRK|ISTRIP|INLCR|IGNCR|ICRNL|IXON`) も落とす**。ICRNL を残すと、ユーザの Enter (`\r`) が端末 driver の段階で `\n` に変換され PTY に届く。`prompt_toolkit` 系の選択ピッカー (`Application` / `questionary` 等) は `Keys.ControlM` (= `\r`) のみを「選択確定」にバインドしているため、CR→NL 変換がかかると Enter が無反応になる (`aws configure sso` のアカウント選択画面で再現)。`c_oflag` (OPOST) は触らない: `show_minibuffer` 等で `writeln!(stdout)` が `\n` のみを書く箇所があり、端末側の NL→CRLF 変換に依存している。
- **入力スレッド再開の 3 状態 (idle / pending / 静音タイマ) は `InputGate` (`src/input_gate.rs`) に集約され、再 arm は `rearm_on_drop()` RAII guard で行う**。「入力スレッドが idle に戻る arm」(AiPrompt / Line / PassthroughEnded) の**入口**で `let _rearm = gate.rearm_on_drop();` を取得すると、continue / break / `?` エラー伝播を含む全スコープ離脱経路で Drop が再 arm する。`arm_passthrough` は private で直接呼べない (旧実装は全 exit point への手書き呼び出しで、呼び忘れ = 入力ハングが 2 回発生した)。PtyData arm (入力スレッド継続中) では guard を取得しないこと。`break 'main_loop` 経路でも guard は発火するが、ループ終了後は `maybe_request_passthrough` が走らないので無害な no-op。発行判定+送信+フラグ遷移は `maybe_request_passthrough()` に一体化されており、ばらの bool を main loop に再導入しないこと。
- **aishプロンプト表示中は PTY出力の画面描画を抑制**（`MINIBUFFER_ACTIVE` フラグ）。ただしリングバッファへの記録は継続する。
- **PTY 出力の吸い出しは全て `pty_drain::drain_pty` (`src/pty_drain.rs`) 経由で行う**。手書きの `while let Ok(data) = pty_rx.try_recv()` ループを main.rs に再導入しないこと。「表示の有無・先頭改行の除去 (`skip_leading_newline` は表示のみ trim) に関わらず、吸い出した data は必ず trim 前の完全な形で ring_buffer に記録される」不変条件は drain_pty 内で一元保証されており、ばらのループに戻すとこの保証が呼び出し側規律に退化する。チャンク内の処理順 (debug → 表示 → flush → 記録 → sniffer) と「表示 write 失敗はそのチャンクを記録せず伝播 (旧コード互換)」も drain_pty の単体テストで固定している。AI 対話直前の打ちかけ消去 + 消去 redisplay の吸収は `discard_stale_readline_input` (`src/conversation.rs`) に関数化されている (Hidden drain で記録のみ)。
- **通常動作中は PTY出力に aish 独自の文字列を一切挿入しない**（パススルーに徹する）。ステータスバーは DECSTBM の外に描画する。
- **Shift+Enter による改行は非対応**。ターミナル間で CSI u / legacy の扱いが揃わないため。改行は `Alt+Enter` のみサポート。
- **bash の打ちかけ入力消去 (Ctrl+A + Ctrl+K = `0x01 0x0b`) は `PtyHandler::kill_line()` / `refresh_prompt()` (`src/pty_handler.rs`) にカプセル化されており、`0x01,0x0b` のリテラルは pty_handler.rs 以外に書かないこと** (grep で機械検査できる状態を保つ)。消去は AI 提案コマンドの「最初の実行」直前 1 回だけ PTY に送る (`src/conversation.rs` の `confirm_and_execute` で、まだ何も実行していないとき = `executed.is_empty()` に `kill_line()`)。show_minibuffer 終了時 (= AI 質問送信時) には送らない。**ただし shell プロンプトをリフレッシュする改行送信 (slash command 処理後 / AI 対話終了後) は必ず `refresh_prompt()` を使い、素の `pty.write(b"\n")` を書かないこと** (refresh_prompt が改行の直前に行消去する。refresh_prompt の消去側 write エラーは意図的に握りつぶし、改行側の Result だけ返す旧コード互換仕様)。これをしないと、ユーザが Ctrl+/ の前に打ちかけた未確定 shell 入力 (passthrough で既に PTY に届き bash readline に残っている) を、リフレッシュ用の改行が勝手に submit して実行してしまう (= ユーザが Enter していない・承認していないコマンドの実行。信頼の根幹に触れる)。AI 提案コマンドを 1 つでも実行した経路では初回実行直前の行消去で既に空行なので no-op。打ちかけが本当に温存されるのは **minibuffer を cancel (ESC/Ctrl+C/Ctrl+/) した場合と空 Enter の場合だけ**で、これらは改行を PTY に送らない経路 (PassthroughEnded / 空 prompt) なので readline 上で編集を継続できる。Ctrl+C (`0x03`) ではなく Ctrl+A+Ctrl+K を使うのは SIGINT を発火させずに行消去だけしたいため (vim/top 等の子プロセスを意図せず kill しない)。bash プロンプト直後 (打ちかけなし) で送っても no-op なので害なし。後続の AI 提案コマンドは前のコマンドが完了して bash プロンプトに戻った後送られるので追加で送らない。bash readline 以外 (vim 子プロセス等) に届くと `^A^K` がリテラル文字として流れる副作用は残るが、SIGINT 直撃よりは穏当な失敗モード。
- **AI 対話を始める直前 (`src/conversation.rs` の `AiConversation::run` 冒頭、`get_unsent_for` の直前) で打ちかけを `0x01,0x0b` で消去してから drain する**。これは「カーソルが bash の readline モデルと同期しているこのタイミング」でしか正しく消せないため必須。`show_minibuffer` が DSR で実カーソルを bash プロンプト位置 (= 打ちかけ末尾) に絶対座標復元した直後で、まだ AI 出力を 1 文字も stdout に描いていない (slash command は上の分岐で `continue` 済み) から、**実カーソル == bash の readline カーソル**。この状態でだけ消去 redisplay (折り返した打ちかけを畳むための cursor-up) が打ちかけ自身の行に正しく当たる。これを入れないと、**打ちかけが端末幅を超えて複数行に折り返している**場合、対話終了後リフレッシュ (`src/conversation.rs` の `run` 末尾の `refresh_prompt`) で送る Ctrl+A に対し bash が `ESC[A` (cursor-up) を折り返し行数ぶん吐き、その上移動が **aish が stdout に出した `Exec?` 行 (bash は与り知らない)** の上で起きてシェルプロンプトが `Exec?` 行を上書きする (旧不具合: 折り返した打ちかけがあると、AI 提案コマンドが 1 行でも複数行でも、n でキャンセルした瞬間にプロンプトがコマンド表示を上書きした)。先に空にしておけば readline バッファは 1 行に収まり、末尾リフレッシュは cursor-up を一切吐かずプロンプトは必ず新しい行に出る。消去 redisplay バイトは **stdout には転送せず drain して捨てる** (折り返した打ちかけは画面/scrollback に残る = ユーザが打った内容の記録として無害。cursor 制御エスケープを今流すと AI 出力の開始位置がずれる)。`ring_buffer` には従来どおり追記し「PTY 出力は全て ring_buffer に入る」不変条件を保つ。`sleep(150ms)` は消去 redisplay の到着待ち (SSH 越しでは取りこぼし得るが、その場合でも次 main loop drain で追従するだけで上書きは起きない)。後段の初回実行直前の行消去と対話終了後リフレッシュの `0x01,0x0b` は空バッファに対する no-op になるが、防御的に残す。**この消去を撤去する場合は折り返し打ちかけ + n キャンセルで `Exec?` 行が上書きされないことを `/tmp` の pyte ドライバ等で必ず再検証すること**。
- **AI 提案コマンドの完了判定は `PromptSniffer` による passive 検出**。コマンドはユーザ承認文字列をそのまま PTY に送り、PTY 出力末尾がプロンプト形 (`[$#>%➜❯»][空白]+`) になり 200ms 静音したら完了。
- **AI 由来の `message` / `commands` は端末描画前に制御文字を caret 可視化する** (`src/ui.rs` の `visualize_control_line`、`print_ai_message` / `print_ai_commands` / `print_single_confirm_prompt` で適用)。`\r` で行頭復帰・`\x1b[2K` で行消去して確認画面の見た目を送信バイトとズラす偽装 (= ユーザが見て承認した物 ≠ 実際に送る物) を防ぐためで、ESC は `^[`、CR は `^M`、TAB は `^I`、NUL は `^@` のように丸見えになる。AI 出力は (プロンプトインジェクション経由で) 未信頼になり得るので、**ここを「生で `println!`」に戻さないこと**。`message` は複数行説明が正当なので `.lines()` 分割は維持し行内のみ可視化する。
- **AI 提案コマンドの制御文字ガードは `VettedCommand` 型 (`src/vetted_command.rs`) に型化されている**。実行ループ先頭 (`src/conversation.rs` の `confirm_and_execute` ループ先頭、`Approval` 分岐より前) で `VettedCommand::vet` が検証し、制御文字 (改行/CR/ESC/NUL/TAB/その他 C0/DEL/C1) を含むコマンドは Y/n/a に載せず `print_rejected_command` で表示して `continue` し、PTY には送らない。検証後は `ui::print_single_confirm_prompt` (表示) と `PtyHandler::send_approved_command` (送信) の両方が `&VettedCommand` しか受け付けないため、**「画面で承認した物 = サーバで実行される物」が型レベルで保たれ、ガードの撤去・迂回は型エラーになる** (vet は検証のみで文字列を一切変形しない。`as_str()` が同一スライスを返すことをテストで固定)。`[a]` (= 残り全部自動承認) 経路もこのガードを通るので、まとめ承認でも漏れない。Y/n/a の 1 確認の結果は `ConfirmDecision` enum (Run / Skip / RunRest / CancelRest)、残り自動承認モードは `Approval` enum (AskEach / All) で表現し、`CancelRest` は即 break するため「自動承認中かつキャンセル済み」の矛盾状態は構造上存在しない (旧実装のばらの bool 3 つを置換。`wait_confirm_decision` は channel 駆動の単体テストあり)。複数行 / heredoc スクリプトの明示承認モードは未実装 (1 コマンド = 1 行を enforce; 多行は AI に配列分割か単一行等価で出させる)。
- **TUI アプリ (vim / less / top 等) 終了後は aish 側から何も出さない**。bash 単体と同じ「alt screen から戻った状態」に任せる。元はステータスバー復旧のため `Ctrl+L` を撃っていたが、ステータスバー廃止 (commit 7d13700) で動機が消えたため一律全廃した経緯がある。再導入する場合は vim insert モード中の誤発火 (バッファに `^L` がリテラル混入) を避けるため、PTY 出力の passive 検出だけで一意に「終了」と判定できる根拠を用意すること。`\x1b[2J`・DECSTBM (`\x1b[..r`)・alt screen 突入 (`\x1b[?1049h` 等) は TUI 動作中にも出るので終了判定には使えない。
- **`show_minibuffer` は入口で DSR (`\x1b[6n`) を投げて cursor row を取得し、`row == rows` (画面下端) のときだけ `\n` を出して scroll 退避する**。画面上半分のときは入口 scroll はしない (上端を削らない)。終了時は `\x1b7` / `\x1b8` の DECSC/DECRC ではなく、取得した `(row, col)` を `\x1b[{row - total_scrolled};{col}H` で **絶対座標指定して cursor を復元する**。`total_scrolled` は **入口 scroll (1 if was_at_bottom else 0) + minibuffer 表示中の grow scroll の累積合計** で、`show_minibuffer` から `read_minibuffer_line` → `redraw_minibuffer` に `&mut u16` で渡して redraw が grow するたびに `delta` ぶん加算していく。`redraw_minibuffer` の grow scroll は **cursor を実画面最下行 (`term_rows`) に置いた LF の全画面 scroll** で行い、**DECSTBM の scroll region は使わない** (minibuffer 表示中は `MINIBUFFER_ACTIVE` で PTY 描画が抑制されるので region で minibuffer 行を守る相手がいない。終了時の `\x1b[r` は防御的リセットのみ)。region scroll にしない理由: **scroll region が全画面でない間のスクロールは、上端から押し出された行を scrollback に保存せず破棄する** のが xterm 系端末 (Ghostty 含む) の標準挙動で、旧実装 (region 内 scroll。さらに初回 grow は region 未設定 + cursor が bottom margin にないため LF が無スクロールで `total_scrolled` だけ過大計上) は「minibuffer が 1 行伸びるたびに直上の bash プロンプト行や画面の行が恒久消失し、終了時 cursor 復元も 1 行ズレる」不具合があった (ユーザ報告: 複数行入力で上の行がどんどん消える)。全画面 scroll なら通常のシェル出力と同じく履歴は scrollback に退避される。**cursor を `term_rows` に置くことと DECSTBM 不使用は不可分** (region を設定したまま `term_rows` に cursor を置くと region 外で LF が一切 scroll しなくなる)。grow scroll は `was_at_bottom` に関わらず発火させる: 画面上半分始まりでも minibuffer は常に画面最下行起点で伸ばす設計なので、scroll しないと直上の行を上書きしてしまう。grow-shrink-grow の再 scroll は **`reserved_rows` (scroll 確保済み行数の高水位マーク。shrink でも減らさない)** で抑止し、shrink 側で `\x1b[2K` クリア済みの空白行へ再 grow するときは scroll せず再描画だけで済ます (= **shrink branch の行クリアはこの空白不変条件を支えるので撤去しないこと**)。`total_scrolled` は全画面 scroll のみの積算なので「実際に画面が上へ動いた行数」と常に一致し、終了時の絶対座標復元が正確になる。saved_row が小さい (clear 直後等) のに grow が多いと `saturating_sub` の clamp でプロンプト行自体が scrollback へ逃げるが、内容は失われない既知の劣化モード。grow/shrink/DECSTBM 非出力のバイト列は `src/ui.rs` の golden test 群 (`minibuffer_grow_*` / `minibuffer_shrink_*`) で固定している。DSR 応答は `\x1b[{row};{col}R` 形式を 80ms timeout で stdin から読む (`query_cursor_position_dsr`); fd 0 は `passthrough_read_raw` が raw モードで握っているが `ManuallyDrop` で借りて `libc::poll` 非ブロッキング読み取りする。応答前にユーザがキーを打った場合は混入バイトを捨てる単純実装 (実害稀)。応答なし端末では `was_at_bottom = false` fallback で安全側 (= 入口 `\n` 不発火、cursor は絶対座標復元なのでズレない)。すべて stdout 専用で PTY には送らないので bash の実行状態には影響しない。alt screen (vim 等) 中に Ctrl+/ で minibuffer を出すと表示が崩れるが、`Ctrl+L` で TUI 側 redraw 可能なため許容仕様。**キャンセル (ESC / Ctrl+C / Ctrl+/ / "exit") 時は minibuffer 跡を clear + cursor 復元したあと、stdout に `\n` を 1 つ追加で出して bash プロンプトと同行混在を回避する**。打ちかけは画面と bash readline に残るので Enter で実行可能 (誤押し保護は維持)。副作用として bash readline の cursor 認識 (= 改行前の位置) と実際の terminal cursor 位置 (= 改行後の 1 行下) がズレるが、bash の echoback は実 cursor 位置で描画されるので typing は新しい行で正しく見え、Enter 時の `\r\n` で再同期される。打ちかけありで cancel した直後の bash readline 編集 (← Backspace / 矢印キー / 行折り返し近辺) では微小な表示ズレが残りうるが、最下行 scroll で履歴消失する原理上不可避な失敗モード。
- **パススルーで PTY に転送する ESC シーケンスは完全な形まで読み切ってからまとめて送る**。CSI (`ESC [ ... <0x40-0x7E>`) だけでなく SS3 (`ESC O <1 byte>`) も同様。途中で分割して 2 回の write になると、受信側 (vim 等) は ESC タイムアウトで別キーと解釈する (例: `ESC O` + `H` → `ESC` + `O` (open line above) と誤解)。Home/End や F1〜F4、アプリケーションカーソルモードの矢印キーが該当。
- **ESC sequence の追加 byte 読みはすべて poll(50ms) 付きにする (`crate::input` の `Fd0Source::read_byte` + `decode_csi`/`decode_ss3`/`decode_utf8`。継続 byte は `POLL_TIMEOUT_MS`、最初の 1 byte だけブロッキング)**。CSI inner loop / SS3 tail のいずれも blocking read にすると、partial sequence (例: マウストラッキング有効中の whiptail でウィンドウフォーカス切替時に断片送信される) で stdin read が固まり、raw mode 下では `Ctrl+C` も単なる `0x03` バイトなので、SIGINT 含めた全キー入力が PTY に到達しなくなる (pi-hole installer + whiptail + Ghostty focus 切替の組み合わせで再現実績あり)。timeout したら溜めた seq_bytes は不完全なまま PTY に転送する (transparent proxy 原則。TUI は不正 CSI を無視/誤描画する程度でリカバするので、入力凍結より穏当)。fail-safe として CSI バッファに長さ上限 (`MAX_SEQ_LEN = 64`) を設けて壊れた入力で seq_bytes が無限に伸びるのを防ぐ。`is_ok()` 判定は `Ok(0)` (EOF / fd 切断) でも通って未初期化 byte を push するため、`Ok(1)` で厳密判定する。
- **ring_buffer の未送信 cursor は backend ごとに独立**。`sent_marks: [u64; BackendKind::COUNT]` で保持し、`get_unsent_for(kind)` / `mark_sent_for(kind)` を経由する。`/ai` 切替時に新 AI が「これまでの会話の続き」を catch-up できるようにするための仕組み。**新規 backend を追加するときは `BackendKind::ordinal()` / `all()` / `COUNT` を必ず更新する**。実行ファイル名が enum 名 (`as_str()`) と異なる backend (例: `cursor` → `cursor-agent`) を追加する場合は `BackendKind::binary()` 側に分岐を追加すること。`factory::check_installed` と spawn は `binary()` を経由するが、設定 / slash command / 表示は `as_str()` (短縮名) を使う。
- **Claude backend の system prompt (`--append-system-prompt`) は初回ターンのみ送られる** (`src/ai/claude.rs`; resume では二重 append を避けるため付けない)。したがって**出力フォーマットや `commands` の入れ方など、毎ターン守らせたいルールは system prompt ではなく `--json-schema` の field description 側に書く** (スキーマは毎ターン送られる)。実例: AI が実行コマンドを message 本文の「提案コマンド:」に書くだけで `commands` を空配列にし Y/n/a に渡らなかったバグは、follow-up が resume ターンで system prompt 不在だったのが原因。`commands` の description に「本文で実行コマンドを出したら配列にも入れる / 提案が無ければ空配列」という**整合性ルール**を書いて毎ターン効かせている。**「commands を空にするな」式の強制文言は付けないこと** (コマンド不要時にモデルが無理にコマンドを捻り出し、余計な Y/n/a 確認が出る逆効果になる)。**もう 1 つの整合性ルールは「独立した複数コマンドを `;` で 1 本に連結させず `commands` 配列の別要素に分割させる」** (codex が `;` 連結 1 要素で返してくる問題への対策)。これは「1 コマンド = 1 行 = Y/n/a の 1 確認単位」(信頼の根幹) を保つためで、共有 system prompt の「応答ルール」(`src/ai/common.rs` の `build_system_prompt`) と Claude の `commands` description (毎ターン用) の両方に同じ文言を入れている。**ただし `&&`/`||` の条件付き実行と、`for`/`while`/`until`/`case`/`if` 等の制御構文に含まれる `;` は 1 コマンドとして維持する例外を必ず併記すること** (分割すると `for i in 1 2 3; do …; done` 等が壊れる)。prompt を簡素化する際にこの例外を落とさないこと。
- **Claude の `--disallowedTools` は `MANDATORY_DENY` (Bash/Edit/Write) を常に union する** (`src/ai/claude.rs` の `effective_disallowed_tools`)。`[ai.claude].disallowed_tools` は単一文字列の全置換なので、空にすると安全集合が丸ごと消える footgun だった。`allow_unsafe_tools = false` (既定) の間は config 値に baseline を必ず混ぜ、`disallowed_tools = ""` でも Bash/Edit/Write は deny される (Read だけは default 由来なので外せる)。`allow_unsafe_tools = true` のときのみ verbatim (AI に shell/編集を許す危険設定; 上級者向け)。さらに **`--disallowedTools` は args の末尾で push する** (`send()`; `--output-format`/`--json-schema`/`extra_args`/`--model`/`--effort` の後)。これは `extra_args = ["--disallowedTools", ""]` 等を後置きされても CLI 後勝ちでこちらが勝ち、baseline を non-removable に保つため。**この末尾 push を前方に戻したり union を外したりしないこと** (extra_args で安全制約を黙って剥がせる状態に逆戻りする)。なお codex/copilot/cursor の deny 系フラグは config からは触れず固定埋め込みなので、同種の baseline 化はしていない (extra_args での `--enable`/`--allow-tool` 後置き上書きは未対策。横展開する場合は同様に安全フラグを extra_args 後へ再配置する)。
- **cursor backend は `--trust` を常時付与する**。cursor-agent の headless モード (`-p`) は `--trust` 無しだと `Workspace Trust Required` で実行を拒否し、stdin は読まずプロンプト表示だけ出して非ゼロ終了する。`AiError::EmptyOutput` / `NonZeroExit` 経由でユーザに見えるが原因が分かりにくいので、config からは指定不可で `src/ai/cursor.rs` の固定引数に埋め込んでいる。`--yolo` / `-f` (Run Everything) は意図しない実行を許してしまうので絶対に付けない。安全側の `--trust` 単体で十分。
- **cursor backend のツール抑制は `--mode plan` + system prompt の二段構え**。cursor-agent には codex の `--disable shell_tool ...` に相当する個別ツール無効化フラグが無いので、`[ai.cursor].mode = "plan"` (default) で read-only / propose-only モードに固定するのが主防御。OS レベル defense-in-depth として `[ai.cursor].sandbox = "enabled"` を併用可。**`mode = ""` (通常モード) は明示的にユーザが選んだときだけ。aish 側の確認 UI を迂回するリスクがあるので推奨しない**。
- **cursor-agent の Free プランでは Named models が使えず `auto` のみ**。`--model sonnet-4` 等を指定すると `Named models unavailable Free plans can only use Auto.` で `EmptyOutput` エラーになる。paid プランなら sonnet-4 等を指定可能。config では `[ai].model = "auto"` を案内している。
- **copilot backend は `-p` フラグを付けない**。`copilot -p <text>` フラグは positional / stdin と排他で、aish のように stdin で prompt を流すと `error: too many arguments. Expected 0 arguments but got 1.` で死ぬ。copilot CLI は stdin を自動検出するので `-p` なしで `run_cli_capture_stdout` 経由の stdin 渡しで動く。これは他の `-p` 必須 backend (claude, cursor) と挙動が逆になるので、`src/ai/copilot.rs` を編集するときは注意。
- **copilot backend のツール抑制は四段構え**: `--allow-all-tools` (非対話必須) + `--deny-tool=shell` + `--deny-tool=write` + `--no-ask-user` + `--mode plan` (default)。deny は allow に優先するので、`--allow-all-tools` を付けても shell 実行と書き込みは完全拒否される。これで copilot は claude / codex 同等の「LLM のみ」状態に退化する。これらは config からは指定不可で固定引数に埋め込んでいる (信頼の根幹に直結するため)。`--yolo` / `--allow-all` (Run Everything 系) は絶対に付けない。
- **copilot の `--output-format json` は JSONL (1 行 1 オブジェクト)**。他の backend のような単一の外側 JSON ではない。`src/ai/copilot.rs::parse_jsonl_envelope` で行ごとに走査し、`type == "assistant.message"` 行の `data.content` (最終応答テキスト) と `type == "result"` 行の `sessionId` (session UUID) を取り出す。ephemeral な delta / status 行 (`session.*`, `assistant.message_delta`, `assistant.reasoning`, `assistant.turn_*` 等) は無視する。複数 `assistant.message` がある場合は最後のものを採用 (一応 multi-turn 想定だが現状は 1 turn のみ)。
- **copilot は組織ポリシーで CLI 利用が拒否されることがある**。`Error: Access denied by policy settings (Request ID: ...)` が出たら個人の Copilot 設定 (https://github.com/settings/copilot) または所属組織の admin に確認が必要。aish 側では `AiError::NonZeroExit { stderr }` 経由でユーザに見えるので、エラー本文の `policy` 文字列でユーザが原因に気付ける。
- **Generic backend (`BackendKind::Generic(u8)`) は `[[ai.providers]]` registry 経由で動的解決される**。registry は `BackendKind::init_generics(&providers)` で **OnceLock に 1 度だけ populate** され、`Box::leak` で `&'static str` (display_name = recipe.name そのまま、binary) と `&'static ProviderRecipe` (recipe) を確保する。プロセス全期間で生存。`as_str()` / `binary()` の戻り型は `&'static str` のまま維持されるので呼び出し側の変更は不要。init 前 / index 範囲外の Generic にはフォールバック (`"?"`) を返すので panic はしないが、spawn は失敗する。新たに native variant を増やす場合は `all_native()` / `NATIVE_COUNT` / `ordinal()` の native 分岐を更新する (Generic は自動)。ユーザは built-in と generic を **同じ flat namespace で参照** する (`aish --ai claude` / `aish --ai ollama-llama`)。`generic:` のような prefix は付けない。
- **ring_buffer の sent_marks は HashMap<usize, u64>** (旧固定長 `[u64; COUNT]` から変更)。Generic を ordinal `6 + idx` で扱うため。entry が無いキーは "まだ何も送ってない" 扱いで 0 と解釈される (=起動以降全部 catch-up)。`mark_sent_all()` は `BackendKind::all_native()` + `BackendKind::all_generics()` 両方を回す。新規 native backend を増やすときは `all_native()` に追加するだけで OK (COUNT 配列サイズ更新の手間が消えた)。
- **`BackendKind::parse(s)` は 2 段階解決**: (1) `parse_native` で built-in 6 種を直接 match、(2) hit しなければ `GENERIC_REGISTRY` を線形検索。registry init 前に provider 名で parse すると native 失敗 → registry 空でも空配列を返すので最終的に `unknown backend` エラー。`main::run()` で `config::Config::load` → `BackendKind::init_generics(&config.ai.providers)` → 以降 backend kind parse という順序を守ること。`validate_providers` が native 予約語 (`all_native().as_str()`) との衝突を起動時に reject するため、有効 config では parse の段階で曖昧性は発生しない (native 優先)。テストでは Generic resolution を直接検証しない (`OnceLock` がプロセス全期間共有なので並列テストで干渉する)。Generic 駆動の動作確認は `src/ai/generic.rs` の単体テストで `Box::leak(Box::new(recipe))` 直書きで行う。
- **Generic backend は aish 側で安全フラグ (`--deny-tool` 等) を自動付与しない**。recipe 著者が `args` に `--mode plan` 等を明示的に含める想定。信頼できない CLI を generic に登録すると、aish の確認 UI を迂回して shell 実行される可能性がある (claude / codex / copilot のような自動 deny は実装していない)。SPEC.md § 6.0 / `[[ai.providers]]` 安全性節を参照。
- **AI 応答受信後の注釈記録は `RingBuffer::record_ai_exchange` (`src/ring_buffer.rs`) 経由で行う**。注釈 (`[aish→<kind>]> ...` / `[ai/<kind>]> ...` / `[ai/<kind> suggests] ...`) の append → `mark_sent_for(current_kind)` の順序不変条件 (**current AI は再受信せず、他 backend は次回 catch-up で受信する**。逆順だと current AI が自分の発話をループで受信する) はこのメソッド内に閉じており、ばらの `append_text` + `mark_sent_for` を main loop に再導入しないこと。注釈ラベルのフォーマットも record_ai_exchange が唯一の定義で、単体テスト (`record_ai_exchange_format_is_stable`) で固定している。
- **`/clear` は ring_buffer.mark_sent_all() で全 backend の cursor を末尾に進める**が、AI CLI 内部の session/history は **current backend のみ** リセットする (他 backend の instance は保持していないため)。「全 AI の会話を仕切り直す」セマンティクスを守りつつ、副作用は最小限にする妥協案。
- **ring_buffer の `[link]` ライクな PTY 文字列と注釈ラベル (`[aish→...]` / `[ai/...]`) は衝突しうる**。AI は文脈で区別する想定。誤検出が頻発したらフォーマットを XML 風 (`<aish to="claude">`) 等に変更する余地あり。

## 開発フロー

ユーザから明示的に依頼された作業を行うときは、以下のフローに従う。これは CLAUDE.md「信頼の根幹」を守るための運用ルールであり、デフォルトの「commit は明示要求があるときだけ」を上書きする。

- **ユーザのプロンプトが疑問文で終わっている場合、それは質問であって作業依頼ではない**。勝手にソースコードを修正してはいけない。まず質問に答え、必要であれば修正案を提示してユーザの指示を待つ。
- **ソースコードを修正する前に、仕様や要件に疑問点があれば全て必ずユーザに確認する**。曖昧な前提のまま作業を進めない。判断に迷ったら手を動かす前に聞く。
- **コードに新しい仕様 / 落とし穴 / 不変条件を導入する変更を行ったら、同じ作業フローの中で CLAUDE.md「実装上の注意」(仕様が膨らんだ場合は SPEC.md) への追記を必ず行う**。対象は、コードから直ちには読み取れず後から見て間違える可能性のある挙動。typo 修正・cosmetic refactor・既存仕様の範囲内の小さな修正は対象外。判断に迷ったら追記する側に倒す。
- **依頼された作業が完了したら、コード変更と CLAUDE.md / SPEC.md の追記を 1 つの commit にまとめて自動作成してよい**。ユーザに「commit しますか？」と都度確認しなくてよい。コミットメッセージは既存スタイル (`Feat:` / `Fix:` / `Refactor:` / `Docs:` / `Chore:` 等の日本語プレフィックス + 短い説明、必要なら body) に従う。
- **push は別途ユーザに確認する**。リモートに公開する操作は引き続き確認が必要。
- **タグ付け / git tag タグをつける。git push --tagsでGitHub Actionが動き出し、リリースされるが、git pushは自動では行わない。

## 設定ファイル
- `~/.aish/config.toml` (TOML)。`--config <path>` で変更可能。
- `config.toml.example` にサンプルあり。
