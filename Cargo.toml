[package]
name = "aish"
version = "0.1.0"
edition = "2021"
description = "CLI SSH + AI (Claude Code)"

[dependencies]
portable-pty = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
strip-ansi-escapes = "0.2"
dirs = "5"
unicode-width = "0.2"
libc = "0.2"
